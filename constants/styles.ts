const padding = 8;

export const STYLES_PADDING = `${padding}px`;
export const STYLES_PADDING_DOUBLE = `${padding * 2}px`
export const STYLES_PADDING_HALF = `${padding / 2}px`

export const TEST = "1px solid red"